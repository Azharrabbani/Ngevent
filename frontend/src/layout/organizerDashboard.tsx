export default function OrganizerDashboard({ children }: { children: React.ReactNode }) {
    return (
        <div className="min-h-screen bg-[#FAF7FF]">
            {children}
        </div>
    )
}